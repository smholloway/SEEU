<h3>Mad-Libs</h3>

<% operators_array = ["", ">", ">=", "=", "<=", "<"] %>
<% values_array = [""] %>
<% sensor_conditionals_array = ["then", "and"] %>
<% actuator_conditionals_array = ["", "and"] %>

<div id="condition1">
  <span class="code">if</span> 
  <%= collection_select(:sensor, :name, Sensor.all, :id, :name, :prompt => true ) %>
  <%= select(:sensor, :operator, operators_array) %>
  <%= select(:sensor, :value, values_array) %>
  <%= select(:sensor, :conditional, sensor_conditionals_array) %>
</div> <!-- condition1 -->

<div id="action1">
  <span class="code">set</span> 
  <%= collection_select(:actuator, :name, Actuator.all, :id, :name, :prompt => true) %>
  <span class="code">to</span>
  <%= select(:actuator, :value, values_array) %>
  <%#= select(:actuator, :conditional, actuator_conditionals_array) %>
</div> <!-- action1 -->

<br/>

<%= form_for @rule, :html => { :id => "madlib_create" } do |f| %>
	<div class="field" style="display:none">
    <%= f.label :rule %><br />
    <%= f.text_area :rule, :rows => 3 %>
  </div>

	<div class="field">
    <%= f.label :description %>: (Optional) Describe what your rule is supposed to do<br/>
    <%= f.text_area :description, :rows => 3 %>
  </div>

  <%= f.submit :name => "Create Madlibs Rule", :valu => "Create Madlibs Rule", :id => "madlib-create" %>
<% end %>
